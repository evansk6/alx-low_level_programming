#include "main.h"
#include <stdio.h>
/**
 * main -> sum of two integer
 * add two integer
 * Return: return always 0
 */
int add(int, int)
{
	int x, y, sum;
	
	sum = x + y;
	printf("\n = %d\n", sum);
	return (0);
}
