#include "main.h"
#include <string.h>

/**
 * _strcmp -> compare
 * @s1: param
 * @s2: param
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
